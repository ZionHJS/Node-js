//node 会把我们自己写的代码自动封装成这么一个东西
(function(exports, require, module, __filename, __dirname) {
    // 这里是你自己写的js代码文件
});

    // 这里是你自己写的js代码文件
    var a = 10;
    console.log('23456');

    console.error(1222222);
    console.log(Object.prototype);
    console.time('123');
    var t = 0;
    for (var i = 0; i < 10000; i++) {
        t += 1;
    }
    console.timeEnd('123');
